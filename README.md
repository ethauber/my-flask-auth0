Create both a standard web application and an API in Auth0

### Command to generate a self-signed certificate for local testing
openssl req -x509 -newkey rsa:4096 -nodes -out cert.pem -keyout key.pem -days 365